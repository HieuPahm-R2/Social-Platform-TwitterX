export enum UserVerifyStatus {
  Unverified,
  Verified,
  Banned
}
export enum TokenType {
  AccesToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerifyToken
}

export enum MediaType {
  Image,
  Video,
  HLS
}